import { css } from '@emotion/react';

const Button = () => {
  return (
    <button
      type="button"
      css={css`
        color: red;
      `}
    >
      ㅎㅇ띠
    </button>
  );
};

export default Button;
